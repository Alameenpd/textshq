import { PlatformAPI, ClientContext, SerializedSession } from '..';
import { Container } from './types';
declare class ContainerizedPlatformAPI implements Partial<PlatformAPI> {
    private readonly containerConstructor;
    private container;
    private readonly requestQueue;
    private readonly callbacks;
    private requestId;
    private callMethod;
    private readonly handleWorkerCleanupMessage;
    private initContainer;
    constructor(containerConstructor: (dataDirPath: string) => Container);
    readonly init: (session: SerializedSession, clientContext: ClientContext) => Promise<void>;
    readonly dispose: () => Promise<void>;
}
export default ContainerizedPlatformAPI;
